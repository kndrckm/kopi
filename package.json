{
  "name": "kopiAPK",
  "version": "1.0.0",
  "description": "Monicoffee Android app with ML Kit background removal",
  "main": "app.js",
  "scripts": {
    "build": "cp *.html *.js *.css *.json *.ico icons -r www/ 2>/dev/null || true && npx cap sync android",
    "android:open": "npx cap open android",
    "android:run": "npx cap run android",
    "sync": "npx cap sync android"
  },
  "keywords": ["capacitor", "android", "ml-kit", "background-removal"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor-mlkit/subject-segmentation": "^8.0.1",
    "@capacitor/android": "^8.2.0",
    "@capacitor/cli": "^7.6.0",
    "@capacitor/core": "^8.2.0",
    "@capacitor/ios": "^8.2.0"
  }
}
